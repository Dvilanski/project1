<%- layout("./layout/layout") %>
    <div class="container">
        <h1>instractor: <%= instractor.name %>
                <%= instractor.lastName %>
        </h1>
        <form action="/instractors/<%= instractor.id %>?_method=DELETE" method="POST">
            <button class="btn btn-primary">DELETE</button>
        </form>
    </div>