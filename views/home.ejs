<%- layout("./layout/layout") %>
    <div class="container">
        <div class=" row">
            <% screens.forEach(element=> { %>
                <div class="col-md-6">
                    <h1>
                        <%= element %>
                    </h1>
                </div>
                <% }) %>
        </div>
    </div>